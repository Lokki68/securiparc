= f.input :email
.grid.grid-cols-2.gap-4
  = f.input :first_name
  = f.input :last_name
.grid.grid-cols-2.gap-4
  = f.input :password
  = f.input :password_confirmation
= f.input :role_id, as: :select, collection: Role.all.map(&:name)
.flex.justify-end.items-center.mt-4
  = link_to 'Annuler', users_path, class: 'btn btn-outline'
  = f.submit 'Enregistrer', class: 'btn btn-contain'