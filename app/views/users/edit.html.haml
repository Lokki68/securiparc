.container-fluid
  .card.col-lg-6.mx-auto
    = render 'shared/card/card_header', title: 'Modifier Utilisateur'
    .card-body
      = simple_form_for @user, url: user_path(@user), data: {turbo: false} do |f|
        != render 'form', f:f 