= render 'shared/modals/modal', header_title: 'Ajouter un emplacement', header_icon: 'fas fa-map-marker-alt', modal_size: 'modal-md' do
  = simple_form_for @location, url: locations_path do |f|
    = render 'form', f:f