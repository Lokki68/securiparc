%aside{class: 'd-flex flex-column flex-shrink-0 p-3 text-white bg-dark bg-gradient '}
  .py-4.text-gray-500 
    .d-flex.justify-content-center.align-items-center.bg-secondary.bg-gradient.rounded-circle.p-2
      = image_tag('logo_provisoire.png', alt: 'Logo', width: 100, height: 100, class: 'mx-auto')

    %nav.mt-2
      %ul.nav.nav-pills.flex-column.mb-auto
        = render NavitemComponent.new(title: 'Tableau de Bord', icon: 'fas fa-tachometer-alt', path: root_path, active: controller_name == 'dashboard')
        = render NavitemComponent.new(title: 'Utilisateurs', icon: 'fas fa-users', path: users_path, active: controller_name == 'users')
        = render NavitemComponent.new(title: 'Vehicules', icon: 'bi bi-truck', path: vehicules_path, active: controller_name == 'vehicules')
  .d-flex.flex-column.align-items-center.justify-content-center.gap-2.mt-auto
    - if current_user
      %p= current_user.full_name
      = render RoleBadgeComponent.new(role: current_user.role.name)
